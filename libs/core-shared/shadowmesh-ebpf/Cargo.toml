[package]
name = "shadowmesh-ebpf-common"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[dependencies]
aya = { version = "0.13", optional = true }
aya-ebpf = { version = "0.1.0", optional = true }
aya-log-ebpf = { version = "0.1.0", optional = true }

[features]
default = ["aya"]
ebpf = ["dep:aya-ebpf", "aya-log-ebpf"]
